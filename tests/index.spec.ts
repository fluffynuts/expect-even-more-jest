import "../src/index";

describe(`expect-even-more-jest`, () => {
    it(`should import expect-more-jest`, async () => {
        // Arrange
        // Act
        expect(() => {
            // intentionally empty
        }).toBeFunction();
        // Assert
    });


});
